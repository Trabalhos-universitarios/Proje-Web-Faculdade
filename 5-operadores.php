<?php

echo '<p><h3>Operadores</h3></p>';
echo '<p>
        Um operador é um elemento que recebe o valor de uma ou mais expressões, e que retorna
        um outro valor. Vejamos a seguir quais são os operadores do PHP.

        <li><b>Operadores aritméticos: </b>servem para efetuar operações de adição(+), subtração(-), 
        multiplicação(*), divisão(/), módulo(%) e exponencial(**).</li>

        <li><b>Operadores de atribuição: </b> servem para atgribuir o valor da expressão à direita para o esperando
        da esquerda(=).</li>

        <li><b>Operadores bit a bit(bitwise): </b>servem para avaliar e modificar bits específicos em um tipo de dados
        inteiro(AND, OR, XOR, NOT, <<"e">>), a letra "e" é sem aspas.</li>

        <li><b>Operadores de comparação: </b>servem para coparar valores iguais (==), idênticos (===), diferentes(!=ou<>),
        não idênticos(!==), menor que (<), maior que(>), menor ou igual (>=). Seu retorno é TRUE ou FALSE.3</li>

        <li><b>Operadores de controle: </b>o sinal de arroba @ serve, quando preceder uma expressão em PHP, para ignorar qualquer mensagem de erro
        gerada pela sua execução.</li>

        <li><b>Operadores de incremento e decremento: </b></li>
      </p>';

?>